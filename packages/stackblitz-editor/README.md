# @x-ude/adapter-stackblitz-editor

StackBlitz-backed editor adapter. Implements `adapters.EditorAdapter` from
`@x-ude/sdk`.

Current state: scaffold only. The production editor port lives in the private
frontend monorepo and is migrated manually per
`MONOREPO_SPLIT_RUNBOOK.md` Phase E.
