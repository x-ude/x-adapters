# x-adapters

PUBLIC — Adapter implementations bridging `@x-ude/sdk` contracts to external systems (IDEs, LLMs, VCS, sandboxes).

Status: bootstrap. See `x-ude/x-docs` → `development/operations/MONOREPO_SPLIT_RUNBOOK.md` §9 (Phase E) for the build-out plan. First adapter: `packages/stackblitz-editor` (port of `StackBlitzEditor` from `ude-frontend-vite-ts`).
